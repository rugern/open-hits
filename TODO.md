1. Add slogan (advise me on where to put it. It is supposed to be a bit funny): 50% less engagement, 100% more luggage
   space!
2. Add button on page for spinning wheel to "Bop it". Should flash the background around the spinning wheel between a
   set of colors.
3. Add search functionality to filter playlists
4. trackCount is always undefined in the playlist component.
5. when clicking "start game", i dont want the spinning wheel to start automatically, but start when clicking the wheel. It should only start when the wheel is clicked.
6. when the wheel is spinning and music paused, it should make a tapping sound as if there are a pointer hitting rods along the side of the wheel
