1. Add button on page for spinning wheel to "Bop it". Should flash the background around the spinning wheel between a
   set of colors.
2. Add search functionality to filter playlists
3. when clicking "start game", i dont want the spinning wheel to start automatically, but start when clicking the wheel. It should only start when the wheel is clicked.
4. when the wheel is spinning and music paused, it should make a tapping sound as if there are a pointer hitting rods along the side of the wheel
5. Is it necessary to have this permission? From spotify auth screen: "• Your name, username, profile picture, Spotify followers, and public playlists."
