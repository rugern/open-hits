1. Add button on page for spinning wheel to "Bop it". Should flash the background around the spinning wheel between a
   set of colors.
2. Add search functionality to filter playlists
3. when the wheel is spinning and music paused, it should make a tapping sound as if there are a pointer hitting rods along the side of the wheel
4. Is it necessary to have this permission? From spotify auth screen: "• Your name, username, profile picture, Spotify followers, and public playlists."
