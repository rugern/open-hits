1. remove the bingo button from the header until the game starts (the host has pressed the "Start game" button)
2. switch the order of the quit and bingo buttons for host. make the bingo button stand out a bit more visually
3. ensure the arrow and "Back" text in the back button are aligned horizontally, i.e. not breaking into two lines
