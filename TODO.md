1. Fix issue where user is not redirected correctly after connecting to spotify. The landing page with "Connect spotify" button is what is shown until the page is refreshed
2. Add slogan (advise me on where to put it. It is supposed to be a bit funny): 50% less engagement, 100% more luggage
   space!
3. Add button on page for spinning wheel to "Bop it". Should flash the background around the spinning wheel between a
   set of colors.
4. Add search functionality to filter playlists
